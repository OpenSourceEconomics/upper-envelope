---
name: upper-envelope
channels:
  - conda-forge
  - nodefaults
dependencies:
  - pip
  - setuptools_scm
  - toml

  # Package dependencies
  - jax
  - numpy
  - numba
  - pyyaml

  # Testing infrastructure
  - pytest
  - pytest-cov
  - pytest-xdist
  - pip:
      # Install locally
      - -e ../
